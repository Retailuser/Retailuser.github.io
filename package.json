{
  "name": "tnb-asset-management",
  "private": true,
  "scripts": {
    "build": "esbuild js/main.js --bundle --format=iife --outfile=js/app.bundle.js --platform=browser"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
